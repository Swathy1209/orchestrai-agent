# OrchestrAI Career Agent — Python Dependencies
# Install with: pip install -r requirements.txt

# ── HTTP & Scraping ───────────────────────────────────────────
httpx>=0.27.0              # Async HTTP client (source fetchers)
beautifulsoup4>=4.12.0     # HTML scraping (Internshala, Unstop, LinkedIn)
lxml>=5.2.0                # Fast HTML parser backend for BeautifulSoup
requests>=2.31.0           # Synchronous GitHub API calls (github_yaml_db.py)

# ── Data Serialisation ────────────────────────────────────────
pyyaml>=6.0.1              # YAML read/write for GitHub cloud database

# ── AI / LLM ─────────────────────────────────────────────────
openai>=1.30.0             # OpenAI-compatible client (used with Gemini 2.0 Flash)

# ── Scheduling & Web ─────────────────────────────────────────
APScheduler>=3.10.4        # Cron-based scheduler (9:30 AM IST daily trigger)
fastapi>=0.111.0            # Web server to expose /trigger /test-email dashboard
uvicorn>=0.30.1             # ASGI server to run FastAPI on Render

# ── Email ─────────────────────────────────────────────────────
# smtplib is stdlib — no install needed
# Resend is called via HTTP (requests) — no extra package needed

# ── Environment ───────────────────────────────────────────────
python-dotenv>=1.0.1       # Load .env file into os.environ

# ── PDF Parsing (Resume Optimization) ────────────────────────
PyPDF2>=3.0.1              # PDF text parser
PyMuPDF>=1.23.0            # Fast PDF parser for ResumeOptimizationAgent

# ── Standard Library (no install needed) ─────────────────────
# asyncio, base64, json, logging, re, smtplib, email, datetime, signal, sys, threading
